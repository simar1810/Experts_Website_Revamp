import ProfileRedirectClient from "./ProfileRedirectClient";

/** Legacy /profile → client dashboard (main site when on shop.*) */
export default function ProfileRedirectPage() {
  return <ProfileRedirectClient />;
}
